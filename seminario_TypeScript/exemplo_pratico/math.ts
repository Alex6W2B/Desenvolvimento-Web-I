const sum = (n1:number, n2:number) => n1 + n2
const minus = (n1:number, n2:number) => n1 - n2
const mult = (n1:number, n2:number) => n1 * n2
const div = (n1:number, n2:number) => n1 / n2

export {sum, minus, mult, div}